def initial_position(x,y,z,dir):
    n=int(input("Print the number of inputs"))
    print("Series of movement forward/backward (f, b),left/right (l, r),up/down (u, d)")
    array=[]
    for i in range(n):
        array.append(input(array[i]))
